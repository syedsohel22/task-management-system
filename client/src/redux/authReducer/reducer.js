const initalstate = {
  isLoading: false,
  isError: false,
  token: "",
  isAuth: false,
};

export const reduer = (state = initalstate, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
